[1mdiff --git a/pages/projects/[id]/kanban.js b/pages/projects/[id]/kanban.js[m
[1mindex 01ca933..4227989 100644[m
[1m--- a/pages/projects/[id]/kanban.js[m
[1m+++ b/pages/projects/[id]/kanban.js[m
[36m@@ -2,6 +2,8 @@[m [mimport { useEffect, useMemo, useState } from 'react';[m
 import { useRouter } from 'next/router';[m
 import Layout from '@/components/Layout';[m
 import { supabaseBrowser } from '@/lib/supabaseClient';[m
[32m+[m[32mimport { useAiUsage } from '@/hooks/useAiUsage';[m
[32m+[m[32mimport AiUsageBanner from '@/components/AiUsageBanner';[m
 import KanbanColumn from '@/components/KanbanColumn';[m
 import PostModal from '@/components/PostModal';[m
 import SchedulePreview from '@/components/SchedulePreview';[m
[36m@@ -18,6 +20,8 @@[m [mexport default function ProjectKanban() {[m
   const r = useRouter();[m
   const { id } = r.query; // project id[m
   const [project, setProject] = useState(null);[m
[32m+[m[32m  // IA usage (depende do agency_id do projeto)[m
[32m+[m[32m  const usage = useAiUsage(project?.agency_id);[m
   const [columns, setColumns] = useState([]);[m
   const [cards, setCards] = useState([]);[m
   const [postsById, setPostsById] = useState({});[m
[36m@@ -147,6 +151,8 @@[m [mexport default function ProjectKanban() {[m
 [m
   return ([m
     <Layout>[m
[32m+[m[32m      {/* Aviso de cota de IA */}[m
[32m+[m[32m      {!usage?.loading && <div className='mb-3'><AiUsageBanner usage={usage} /></div>}[m
       <div className="flex items-center justify-between mb-4">[m
         <h1 className="text-2xl font-bold">Kanban do Projeto</h1>[m
         <div className="flex items-center gap-2">[m
[36m@@ -401,10 +407,12 @@[m [mfunction AIGenerator({ project, onInserted }) {[m
   return ([m
     <div className="mt-8">[m
       <button [m
[31m-        className="border rounded px-3 py-2 hover:bg-neutral-50" [m
[32m+[m[32m        className="disabled:opacity-50 disabled:cursor-not-allowed border rounded px-3 py-2 hover:bg-neutral-50"[m[41m [m
         onClick={() => setOpen(true)}[m
[32m+[m[32m        disabled={usage?.level === 'block'}[m[41m [m
[32m+[m[32m        title={usage?.level === 'block' ? 'Sem cota de IA: ajuste plano ou aguarde próximo mês' : undefined}[m
       >[m
[31m-        Gerar cronograma com IA[m
[32m+[m[32m        Gerar conteúdo com IA {usage?.quota ? `(${usage.used}/${usage.quota})` : ''}[m
       </button>[m
       [m
       {open && ([m
[36m@@ -412,11 +420,7 @@[m [mfunction AIGenerator({ project, onInserted }) {[m
           <div className="bg-white w-full max-w-3xl rounded-xl shadow-lg overflow-hidden max-h-[90vh] flex flex-col">[m
             <div className="px-4 py-3 border-b flex items-center justify-between">[m
                   <h3 className="font-semibold">Gerar cronograma (IA)</h3>[m
[31m-                  {usage && ([m
[31m-                    <div className="mt-2">[m
[31m-                      <UsageBanner usage={usage} onManage={() => window.location.href = '/settings/plan'} />[m
[31m-                    </div>[m
[31m-                  )}[m
[32m+[m[32m                  {!usage?.loading && <div className='mb-2'><AiUsageBanner usage={usage} /></div>}[m
               <div className="flex items-center gap-2">[m
                 <Link href="/help/cronogramas" className="text-sm underline">Ajuda</Link>[m
                 <button [m
